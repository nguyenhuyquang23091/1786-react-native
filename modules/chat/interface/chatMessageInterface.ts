export interface FirestoreMessage {
  message: string;           
  platform: string;         
  senderId: string;        
  senderName: string;      
  senderRole: string;     
  timestamp: number;
}